#include<stdio.h>
int main(){
	int a[10],pos,element,n=5;
	 for (int i=0;i<n;i++){
	 	printf("enter a number for a[%d]",i);
	 	scanf("%d",&a[i]);
		 }
	 	
	 	printf("enter position to dlt element");
	 	scanf("%d",&pos);
	 	
	for (int i=pos-1;i<n;i++){
		a[i]=a[i+1];}
		n--;
    for(int i=0;i<n;i++){
    	printf("%d",a[i]);
	}
	return 0;
	 }
	 
